extends layout

include mixins/_appNav

block content
  +appNav(title, menus.settingsCategories.appNavLeft, menus.settingsCategories.appNavRight)

  div.app-page
    if categories
      each group in categories
        .transactions__group
          h4.transactions__group__title= group._id
          ul.transactions__list
            each category in group.categories
              li.content__item
                a(href=`/settings/category/${category._id}/edit`)
                  span.content__item__name
                    span.transaction__pill(style=`background-color: ${category.color}`)= `${category.name} (${category.count})`
                  span.content__item__action
                    i(data-feather="chevron-right")
